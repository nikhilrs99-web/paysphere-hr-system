package com.hrproject.employeedb.repository;

import com.hrproject.employeedb.model.TalentProgram;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TalentProgramRepository extends JpaRepository<TalentProgram, Long> {
}
